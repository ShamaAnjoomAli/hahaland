Ascent of Ra assets v2 — visible platform fix

Replace the existing files in:
public/assets/minigames/stairway_to_the_sun/

The transparent gameplay assets are tightly cropped so Phaser scales the actual artwork instead of a mostly empty 512x512 canvas.
Filenames and texture keys are unchanged.
